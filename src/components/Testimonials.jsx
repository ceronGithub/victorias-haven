/**
 * Testimonials.jsx
 * Section 8 — Guest Stories.
 * Currently empty — no testimonials yet.
 * Section is hidden from view until real guest reviews are added.
 */

export default function Testimonials() {
  return null;
}
